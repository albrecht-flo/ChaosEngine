#include "UISystem.h"

#include "core/utils/Logger.h"
#include "Engine/src/core/Components.h"

using namespace ChaosEngine;

void UISystem::init(ECS &ecs) {

}

void UISystem::update(ECS &ecs) {
    // TOBE
//    auto scripts = ecs.getRegistry().view<Transform, UIComponent>();

//    for (auto&&[entity, transform, text]: scripts.each()) {
    // Handle user input and dispatch events for UI Component
//    }

}
